name: cellular-automaton-dsl
version: 0.1.0.0
dependencies:
  - base >=4.7 && <5
  - parsec
  -

library:
  source-dirs: src

executables:
  cellular-automaton-dsl-exe:
    main: Main.hs
    source-dirs: app
